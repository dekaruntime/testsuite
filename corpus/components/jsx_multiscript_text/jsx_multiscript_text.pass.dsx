import { echo } from "io"

const el = <p>Café Yirgacheffe — 18,50 € — don't miss it: $18.50 / £15 #1 @ 100% · 日本語 · Ελληνικά · Русский · العربية · የቡና ☕ 🎉 café</p>
const result = unsafe { deka.ui.renderToString(el) }
match (result) {
  Ok(r) => echo(r.html),
  Err(e) => echo(e),
}

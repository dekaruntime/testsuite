import { echo } from "io"
import { jsx } from "ui/jsx"
import { renderToString } from "ui/server"
fn Boom() Component {
  deka.panic("boom")
}
const html = renderToString(jsx(Boom, {}))
echo(html.html)

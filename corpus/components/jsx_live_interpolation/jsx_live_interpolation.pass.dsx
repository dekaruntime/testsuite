import { echo } from "io"

interface RenderResult {
  html: string;
}

const n = 7
const el = <span>{n}</span>
const result = unsafe<RenderResult> { deka.ui.renderToString(el) }
match (result) {
  Ok(r) => echo(r.html),
  Err(e) => echo(e.message),
  _ => echo("nomatch"),
}

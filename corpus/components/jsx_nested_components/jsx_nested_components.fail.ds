import { echo } from "io"
import { jsx, jsxs } from "ui/jsx"
import { renderToString } from "ui/server"
fn Outer() Component {
  return jsxs("div", {children: [jsx(Inner, {text: "a"})]})
}
const html = renderToString(jsx(Outer, {}))
echo(html.html)

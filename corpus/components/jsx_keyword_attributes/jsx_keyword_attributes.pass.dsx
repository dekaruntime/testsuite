import { echo } from "io"

const form = <form><label for="email">Email</label><input type="text" id="email" /><button type="submit">Subscribe</button></form>
const result = unsafe { deka.ui.renderToString(form) }
match (result) {
  Ok(r) => echo(r.html),
  Err(e) => echo(e),
}

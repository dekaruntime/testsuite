{
  "title": "Rendering an undefined component fails",
  "stage": "parse",
  "expectedDiagnosticContains": "unknown identifier `Missing`",
  "notes": "v2 catches the undefined component at compile time rather than at render.",
  "hosts": [
    "browser"
  ]
}

{
  "title": "Undefined variable in JSX attribute expression fails at runtime",
  "stage": "parse",
  "notes": "v2 catches the undefined variable at compile time rather than at runtime.",
  "hosts": [
    "browser"
  ],
  "expectedDiagnosticContains": "unknown identifier `missing`"
}

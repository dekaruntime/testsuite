{
  "title": "Fragment with undefined child fails",
  "stage": "parse",
  "expectedDiagnosticContains": "unknown identifier `missing`",
  "notes": "v2 catches the undefined child variable at compile time rather than at runtime.",
  "hosts": [
    "browser"
  ]
}

{
  "title": "Duplicate JSX attribute with undefined value fails",
  "stage": "parse",
  "notes": "The second duplicate attribute references an undefined variable, causing a runtime failure. v2 catches the undefined variable at compile time rather than at runtime.",
  "hosts": [
    "browser"
  ],
  "expectedDiagnosticContains": "unknown identifier `missing`"
}

# Fixtures that are known to fail, one slug per line.
#
# This file is a RATCHET, not a suppression list. The runner enforces both
# directions:
#
#   * a fixture here that fails  -> reported as "known", does not fail CI
#   * a fixture here that PASSES -> hard error; delete the line
#   * a fixture NOT here that fails -> normal failure
#
# So the list can only shrink. It exists because dsc#80 wired the testsuite
# into CI for the first time; everything below was invisible before that.
#
# Every line must be traceable to an issue. Do not add a line to make a
# build green; fix the fixture or file the bug.

# Expects pre-RFD-24 island HTML (no data-deka-id marker, prerendered signal
# state); current runtime stamps the marker and renders the live signal read
# empty. Needs a deka-side decision on prerendering. dekaruntime/dsc#82
components-interactive-component-with-client-load

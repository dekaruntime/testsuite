import { echo } from "io"
async fn double(x: number) Promise<number> {
  return x * 2
}
async fn main() {
  const arr = [1, 2, 3]
  for (const x of arr) {
    echo(string(await double(x)))
  }
}
await main()

import { echo } from "io"
async fn one() Promise<number> {
  return 1
}
async fn two() Promise<number> {
  return 2
}
async fn main() {
  const x = await one() + await two()
  echo(string(x))
}
await main()

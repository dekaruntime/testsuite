{
  "title": "TypeScript arrow function type is rejected",
  "stage": "parse",
  "expectedDiagnosticContains": "expected ``)``, found ``:``",
  "notes": "Pins the current diagnostic. `(n: number) => number` should be rewritten as `fn(number) number`."
}

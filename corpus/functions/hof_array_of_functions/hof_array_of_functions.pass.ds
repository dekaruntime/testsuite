import { echo } from "io"
fn add1(n: number) number {
  return n + 1
}
fn mul2(n: number) number {
  return n * 2
}
const ops = [mul2, add1]
let n = 15
if (ops.has(0)) { n = ops[0](n) }
if (ops.has(1)) { n = ops[1](n) }
echo(string(n))

enum Colour {
  Red,
  Green,
  Blue,
}

// `Red` names a case, so it covers that case and no more. It used to be a
// binding, which counted as a catch-all and made this compile (deka#450).
fn warmth(c: Colour) string {
  return match (c) {
    Red => "r",
    Blue => "b",
  }
}

enum Colour {
  Red,
  Green,
  Blue,
}

// `Red | Green` covers exactly those two -- it is not a catch-all, so `Blue`
// is still missing.
fn warmth(c: Colour) string {
  return match (c) {
    Red | Green => "warm",
  }
}

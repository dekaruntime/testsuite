import { echo } from "io"
const arr = [1, 2, 3, 4]
const sub = arr.slice(1, 3)
const len: number = sub.length
const first: number = sub.has(0) ? sub[0] : 0
const second: number = sub.has(1) ? sub[1] : 0
echo(string(len))
echo(string(first))
echo(string(second))

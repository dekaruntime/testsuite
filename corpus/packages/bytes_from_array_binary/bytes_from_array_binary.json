{
  "title": "bytes.from_array preserves non-UTF-8 bytes from the package",
  "stage": "run",
  "notes": "Native-only. Code lives in the bytes package from the index (`deka add bytes`), not in this fixture.",
  "hosts": [
    "native"
  ],
  "packages": [
    "bytes"
  ]
}

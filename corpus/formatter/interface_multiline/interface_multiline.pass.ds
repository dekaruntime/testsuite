import { echo } from "io"
interface Greeter {
  fn greet() string;
}
struct Person {
  name: string;
}
fn (p Person) greet() string {
  return "Hello, " + p.name
}
const g = Person { name: "Deka" }
echo(g.greet())

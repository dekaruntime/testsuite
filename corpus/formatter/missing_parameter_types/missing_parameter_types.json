{
  "title": "Missing parameter type annotations are rejected",
  "stage": "parse",
  "notes": "DekaScript function parameters require explicit type annotations.",
  "expectedDiagnosticContains": "parameter `x` is missing a type annotation"
}

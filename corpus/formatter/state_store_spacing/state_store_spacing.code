import { echo } from "io"
import { signal } from "ui/reactive"
const countPair = signal(0)
const getCount = countPair[0]
const setCount = countPair[1]
const increment = fn() void { setCount(getCount() + 1) }
increment()
echo(getCount())

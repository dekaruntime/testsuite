import { echo } from "io"
enum Color {
  Red,
  Green,
  Blue,
}
fn name(c: Color) string {
  return match (c) {
    Red => "red",
    Green => "green",
    Blue => "blue",
  }
}
echo(name(Color.Green))

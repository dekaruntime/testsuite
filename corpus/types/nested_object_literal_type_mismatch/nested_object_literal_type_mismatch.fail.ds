import { echo } from "io"
interface Point {
  x: number;
  y: number;
}
interface Line {
  start: Point;
  end: Point;
}
fn draw(l: Line) {
  echo(l.start.x)
}
draw({start: {x: 0, y: "bad"}, end: {x: 1, y: 1}})

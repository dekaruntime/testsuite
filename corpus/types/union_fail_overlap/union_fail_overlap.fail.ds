// deka#530, rfd#42: overlapping members are rejected at declaration.
// `User` satisfies `Named`, so a value could match both predicates and
// narrowing would be ambiguous.
struct User {
  name: string;
}

interface Named {
  name: string;
}

const v: User | Named = User { name: "a" }

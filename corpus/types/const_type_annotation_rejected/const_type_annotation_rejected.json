{
  "title": "Type annotation on a const binding",
  "stage": "run",
  "notes": "Binding type annotations are supported as of deka #273. They were rejected because DekaScript const rode the PHP ClassConst node and let rode StaticVar, neither of which has a type position, so the parser demanded `=` after the name. This fixture has no output; it exists to prove the declaration compiles."
}

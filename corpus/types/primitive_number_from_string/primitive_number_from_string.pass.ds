import { echo } from "io"

const good = parseNumber("3.14")
match (good) {
  Some(v) => echo(string(v)),
  None => echo("none"),
}
const bad = parseNumber("abc")
match (bad) {
  Some(v) => echo(string(v)),
  None => echo("none"),
}

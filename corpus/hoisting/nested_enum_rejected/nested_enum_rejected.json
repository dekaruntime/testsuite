{
  "title": "Nested enum declaration is rejected",
  "stage": "parse",
  "expectedDiagnosticContains": "enum declarations are only allowed at the top level in DekaScript",
  "notes": "DekaScript declarations that the emitter hoists must not appear inside blocks."
}

import { echo } from "io"
const app = <Card />
fn Card() Component {
  return <div>hi</div>
}
const result = unsafe { deka.ui.renderToString(app) }
match (result) {
  Ok(r) => echo(r.html),
  Err(e) => echo(e),
}

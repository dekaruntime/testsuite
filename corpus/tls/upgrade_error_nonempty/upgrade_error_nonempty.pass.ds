import { echo } from "io"
import { upgrade } from "tls"
const msg = match (upgrade(0, "localhost")) {
  Ok(h) => "",
  Err(e) => match (unsafe<string> { String(e) }) {
  Ok(s) => s,
  Err(err) => "",
},
}
echo(string(msg.length > 0))

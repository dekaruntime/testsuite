import { echo } from "io"
interface Config {
  label: Option<string>;
}
fn show(c: Config) {
  echo(match (c.label) {
    Some(v) => v,
    None => "default",
    _ => "?",
  })
}
show({label: Option.Some("hi")})
show({label: Option.None})

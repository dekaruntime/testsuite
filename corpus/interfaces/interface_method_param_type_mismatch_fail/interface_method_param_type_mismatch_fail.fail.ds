import { echo } from "io"
interface Shouter {
  fn shout(msg: string) void;
}
struct Bullhorn {}
fn (b Bullhorn) shout(msg: number) {
  echo(msg)
}
fn runShouter(s: Shouter) {
  s.shout("hey")
}
runShouter(Bullhorn {  })

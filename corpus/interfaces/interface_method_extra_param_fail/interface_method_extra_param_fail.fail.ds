import { echo } from "io"
interface Greeter {
  fn greet() string;
}
struct Person {
  name: string;
}
fn (p Person) greet(extra: string) string {
  return p.name
}
fn welcome(g: Greeter) {
  echo(g.greet())
}
welcome(Person { name: "Deka" })

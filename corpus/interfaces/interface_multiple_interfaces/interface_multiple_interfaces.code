import { echo } from "io"
interface Named {
  name: string;
}
interface Aged {
  age: number;
}
struct Person {
  name: string;
  age: number;
}
fn describe(n: Named, a: Aged) {
  echo(n.name + " " + a.age)
}
const p = Person { name: "A", age: 1 }
describe(p, p)

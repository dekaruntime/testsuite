import { echo } from "io"
import { parse_url } from "http"
const u = match (parse_url("https://example.com/")) {
  Ok(v) => v,
  Err(e) => {host: "", path: "", port: 0, scheme: ""},
}
echo(match (unsafe<string> { String(u.scheme) }) {
  Ok(v) => v,
  Err(e) => "",
})

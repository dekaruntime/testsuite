import { echo } from "io"
import { parse_url } from "http"
const u = match (parse_url("https://example.com/path")) {
  Ok(v) => v,
  Err(e) => {host: "", path: "", port: 0, scheme: ""},
}
echo(match (unsafe<string> { String(u.host) }) {
  Ok(v) => v,
  Err(e) => "",
})

import { echo } from "io"
import { parse_url } from "http"
const u = match (parse_url("http://example.com:8080/x")) {
  Ok(v) => v,
  Err(e) => {host: "", path: "", port: 0, scheme: ""},
}
echo(string(match (unsafe<number> { Number(u.port) }) {
  Ok(v) => v,
  Err(e) => 0,
}))

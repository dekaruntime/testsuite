import { echo } from "io"
export async fn later(value: number) Promise<number> {
  return value
}
export fn plain(value: number) number {
  return value
}
const n = await later(7)
echo(string(n))
echo(string(plain(2)))

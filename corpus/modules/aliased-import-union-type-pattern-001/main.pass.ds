import { echo } from "io"
import { User as Person } from "./types.ds"
fn describe(e: Person | number) string {
  return match (e) {
    Person(p) => p.greet(),
    number(n) => string(n),
  }
}
const p = Person { name: "Ada" }
echo(describe(p))
echo(describe(7))

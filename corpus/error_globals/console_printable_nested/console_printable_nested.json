{
  "title": "console.log recurses through Option<Array<struct>>",
  "stage": "run",
  "hosts": [
    "native"
  ],
  "notes": "Printable category: nested. Some(...) is erased, so the array inside prints on its own (rfd#44, formatting erased values)."
}

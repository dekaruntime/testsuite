{
  "title": "Array.sort on a const array fails",
  "stage": "parse",
  "notes": "The checker rejects mutation before emission.",
  "expectedDiagnosticContains": "cannot call mutable method `sort` on an immutable receiver"
}

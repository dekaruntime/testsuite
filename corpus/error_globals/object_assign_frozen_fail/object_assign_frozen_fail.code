// `Object` is not a name in DekaScript. Reaching it in plain code is a
// diagnostic, not a silent ambient (deka#378, RFD 21 / RFD 13 P10).
const a = {x: 1}
const b = {y: 2}
const c = Object.assign(a, b)

const ui = globalThis.deka.ui
fn Card(props: object) object {
  return ui.jsx("div", { children: props.children })
}
const html = ui.renderToString(ui.jsx(Card, { class: "bg-blue-300 w-[50%] p-4", children: "Hello" }))
console.log(html.html)

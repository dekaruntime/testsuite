const some = Option.Some(7)
const none = Option.None
const a = match (some) {
  Some(v) => v * 2,
  None => 0,
  _ => -1,
}

const b = match (none) {
  Some(v) => v * 2,
  None => 0,
  _ => -1,
}

console.log(a)
console.log(b)

enum Color {
  Red,
  Green,
  Blue,
}

fn name(c: Color): string {
  return match (c) {
    Color.Red => "red",
    Color.Green => "green",
    Color.Blue => "blue",
  }
}

console.log(name(Color.Green))

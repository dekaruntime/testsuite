{
  "title": "Arrow-function callback is rejected",
  "stage": "parse",
  "expectedDiagnosticContains": "Missing semicolon",
  "notes": "Pins the current diagnostic. A JS/TS developer writing `(n) => n + 1` should be told arrows are not DekaScript, not that a semicolon is missing."
}

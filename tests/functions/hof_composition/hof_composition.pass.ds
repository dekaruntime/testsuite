fn compose(f: fn(number) number, g: fn(number) number) fn(number) number {
  return fn(x: number) number {
    return f(g(x))
  }
}
fn double(n: number) number {
  return n * 2
}
fn inc(n: number) number {
  return n + 1
}
console.log(compose(inc, double)(5))

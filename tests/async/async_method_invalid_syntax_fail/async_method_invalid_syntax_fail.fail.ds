struct Store {
  count: number
}
fn (s Store) async load() Promise<number> {
  return s.count
}
const store = Store { count: 5 }
async fn main() {
  const v = await store.load()
  console.log(v)
}
main()

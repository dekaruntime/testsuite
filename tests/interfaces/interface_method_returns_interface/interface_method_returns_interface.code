interface Counter {
  value: number;
  fn next() Counter {}
}

struct Step {
  value: number;
}

fn (s Step) next() Counter {
  return Step { value: s.value + 1 }
}

fn run(c: Counter) {
  const n = c.next()
  console.log(n.value)
}

run(Step { value: 0 })

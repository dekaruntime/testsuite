interface Counter {
  fn increment() void
}
struct Clicker {
  count: number
}
fn (c mut Clicker) increment() void {
  c.count = c.count + 1
}
fn bump(i: Counter) {
  i.increment()
}
const c = Clicker { count: 0 }
bump(c)

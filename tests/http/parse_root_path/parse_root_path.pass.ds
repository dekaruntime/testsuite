import { parse_url } from "http"
const u = match (parse_url("http://example.com")) {
  Ok(v) => v,
  Err(e) => { host: "", path: "", port: 0, scheme: "" }
}
console.log(match (unsafe { String(u.path) }) {
  Ok(v) => v,
  Err(e) => ""
})

import { listen, set_deadline, close } from "tcp"
const h = match (listen("127.0.0.1", 0)) {
  Ok(v) => v,
  Err(e) => -1
}
if (h == -1) {
  console.log("err")
} else {
  const d = match (set_deadline(h, 0)) {
    Ok(v) => "ok",
    Err(e) => "err"
  }
  close(h)
  console.log(d)
}

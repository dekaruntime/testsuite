import { write } from "tcp"
fn utf8(value: string): bytes {
  let encoded = unsafe { new TextEncoder().encode(value) }
  return match (encoded) {
    Ok(buf) => buf,
    Err(err) => utf8("")
  }
}
console.log(match (write(0, utf8(""))) {
  Ok(v) => "ok",
  Err(e) => "err"
})

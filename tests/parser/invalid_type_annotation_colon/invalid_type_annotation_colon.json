{
  "title": "A binding annotation with no type is rejected",
  "stage": "parse",
  "expectedDiagnosticContains": "Expected a type after ':' in this binding",
  "notes": "Stays a rejection. `const x: = 5` must not compile as an untyped binding -- the annotation would look present and check nothing. This fixture caught exactly that hole in the first cut of deka #273."
}
